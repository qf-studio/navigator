# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
coverage/
.nyc_output/

# Build outputs
dist/
build/
*.log

# Temporary files
tmp/
temp/
*.tmp

# Local configuration (don't commit personal settings)
.agent/.jitd-config.local.json

# Context markers (local session state)
.agent/.context-markers/

# User profile (personal preferences, not shared)
.agent/.user-profile.json

# Hook state files (local session state, regenerated each turn)
.agent/.nav-workflow-state.json
.agent/.nav-profile-sync-state.json
.agent/.nav-read-counter.json
.agent/.marker-log

# v7 dispatcher state (local runtime state, TASK-60)
.agent/.nav-runtime-state.json
.agent/.nav-runtime-state.lock
.agent/.nav-dispatch-health.json

# Keep these in git (share with team)
# .agent/.jitd-config.json
# .agent/DEVELOPMENT-README.md
# .agent/tasks/
# .agent/system/
# .agent/sops/

# Marketing/social media content
docs/X-THREAD*.md
docs/PITCH.md
ANNOUNCEMENT-*.md
__pycache__/
social/
skills/nav-social-post/
docs/social/

# Workshop materials (event-specific, kept local)
workshop/
.agent/.features-shown-*
# nav-deep-research fetched bodies (third-party text; rebuild with source_store.py refetch)
.agent/research/*/sources/

# Real-session judge eval prompts (TASK-80 A): private, never commit
hooks/nav_hook_lib/fixtures/judge_eval_real*.json
